<!DOCTYPE html>
<html lang="en">
    <head>
        <meta http-equiv="content-type" content="text/html; charset=utf-8">
        <title>Assets</title>
    </head>
    <body>
        <p>Assets</p>

        <% for (pos, asset) in assets.iter().enumerate() { %>
            <% if pos.rem_euclid(2) == 0 { %>
                <p style="color:gray;"><i><%= asset.name %></i></p>
            <% } else { %>
                <p><i><%= asset.name %></i></p>
            <% } %>
        <% } %>
        <p><a href="/assets/add">Add Asset</a></p>
        <p><a href="/">&lt;- Back</a></p>
    </body>
</html>